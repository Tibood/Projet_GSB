services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gsb_app
    volumes:
      - .:/var/www/html
    environment:
      - DB_HOST=db
      - DB_USER=${DB_USER}
      - DB_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
      - DB_NAME=${DB_NAME}
    networks:
      - app_network
    depends_on:
      - db
    command: sh -c "cd /var/www/html/bin/gendatas && php majGSB.php && php-fpm"


  nginx:
    image: nginx:latest
    container_name: nginx_server
    restart: unless-stopped
    depends_on:
      - app
    ports:
      - 80
      - 443
    networks:
      - app_network
    volumes:
      - .:/var/www/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf

  db:
    image: mariadb:latest
    container_name: mariadb
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    ports:
      - "3306:3306"
    networks:
      - app_network
    volumes:
      - db_data:/var/lib/mysql
      - ./resources/bdd/global.sql:/docker-entrypoint-initdb.d/global.sql

  # phpmyadmin:
  #   image: phpmyadmin/phpmyadmin
  #   container_name: phpmyadmin_server
  #   ports:
  #     - "8081:80"
  #   environment:
  #     PMA_HOST: db
  #     MYSQL_ROOT_PASSWORD: root
  #   depends_on:
  #     - db
  #   networks:
  #     - app_network

networks:
  app_network:
    driver: bridge

volumes:
  nginx-config: {}
  nginx-html: {}
  db_data: